{{ if .Values.__registry__.certCA }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: registryca
  namespace: {{ .Release.Namespace }}
data:
  registry-ca.pem: |-
{{ .Values.__registry__.certCA | nindent 4 }}
{{ end }}
