{{- if eq .Values.networkPolicy "isolated" }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: np-{{ .Values.__workspace__ }}-isolated
  namespace: {{ .Values.__namespace__ }}
  labels:
    {{- include "workspace.labels" . | nindent 4 }}  
spec:
  podSelector:
    matchLabels:
      app: k8shell-workspace
      org: {{ .Values.__organization__ }}
      workspace: {{ .Values.__workspace__ }}
  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: k8shell-proxy 
  egress:
{{- if .Values.allow_egress }}
{{- range $cidr := .Values.allow_egress }}
    - to:
        - ipBlock:
            cidr: {{ $cidr }}
{{- end }}
{{- end }}
{{ include "default.egress" . | nindent 4 }}  
{{- end }}