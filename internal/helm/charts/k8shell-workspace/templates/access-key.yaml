apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.__workspace__ }}-access-keys
  namespace: {{ .Release.Namespace }} 
  labels:
    {{- include "workspace.labels" . | nindent 4 }}  
    type: access-key
data:
  a1key: "{{ .Values.__a1key__ }}"
